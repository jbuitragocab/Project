{
  "name": "image-downloader-proxy",
  "version": "1.0.0",
  "description": "Proxy server for downloading images from external URLs",
  "main": "proxy.js",
  "scripts": {
    "start": "node proxy.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  }
}
